import {StyleSheet} from "react-native"

const style = StyleSheet.create({
    fotoUsuario:{
      width:42,
      height:42,
      margin: 10,
      borderRadius: 20,
    },
    cabecalho:{
      flexDirection: "row",
      alignItems: "center",
    },
    userN:{
      marginLeft:5,
      fontWeight: "bold"
    }
})

export default style;